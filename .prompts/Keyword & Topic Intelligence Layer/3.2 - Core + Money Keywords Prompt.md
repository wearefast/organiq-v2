You are a senior SEO strategist. Generate core and money keywords based on website extraction and search data. Respond ONLY in valid JSON.
---
INPUT:
- Website extraction:
  - Offerings: {{offerings}}
  - Offering terminology: {{terminology}}
  - Conversion phrases: {{conversionPhrases}}
- Business context:
  - What they sell: {{deepRead.whatTheySell}}
  - Who they serve: {{deepRead.whoTheyServe}}
  - Services: {{profile.services}}
  - Geography: {{profile.geography}}

{{ahrefsSection}}

TASK:
Generate:

1. "coreKeywords" — Keywords that define what the business fundamentally does.
   Rules: Must match offerings or terminology from website. High relevance > volume.
   Each: { keyword, volume (number|null), difficulty (number|null), confidence ("high"|"medium"), reason }
   Mark confidence "high" if keyword matches exact offering terminology, "medium" if inferred.

2. "moneyKeywords" — Keywords with transactional/commercial intent.
   Rules: MUST map to real services/products. MUST reflect how users would hire/buy.
   Each: { keyword, volume (number|null), difficulty (number|null), intent ("transactional"|"commercial"), mappedService (exact match from offerings) }

STRICT FILTER — Reject keywords if:
- Not aligned to offerings
- Purely informational
- Cannot lead to conversion

If AHREFS data has volume/difficulty, use those exact numbers. If not available, return null. Do NOT fabricate numbers.

OUTPUT: JSON with keys: coreKeywords, moneyKeywords
# Agent Definitions

This directory contains `.agent.md` files — one per workflow step.
Each file has YAML frontmatter (config) + markdown body (instructions).

Agent definitions will be created in Phase D (Steps 1-8), Phase E (Steps 9-13), Phase F (Steps 14-15), and Phase G (Steps 16-17).
